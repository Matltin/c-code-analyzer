// Demo entry point for Phase 3 project analysis.
int add(int left, int right);
int factorial(int value);

int main(void) {
    int total = add(2, 3);
    int discarded = 0;
    discarded = 10;
    puts("phase three");
    return factorial(total);
    total = 0;
}
